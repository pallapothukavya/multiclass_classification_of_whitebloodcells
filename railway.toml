[build]
builder = "DOCKER"

[deploy]
startCommand = "sh -c 'gunicorn WBC_Classification.wsgi:application --bind 0.0.0.0:${PORT} --timeout 300 --log-level warning --access-logfile -'"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
