[build]
builder = "DOCKER"

[deploy]
startCommand = "sh -c 'gunicorn WBC_Classification.wsgi:application --bind 0.0.0.0:${PORT:-8000} --timeout 300 --access-logfile -'"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
